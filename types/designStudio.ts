type Sticker = {
  x: number;
  y: number;
  src: string;
  id: string;
  scale?: number; // Optional scale property
  rotation?: number; // Optional rotation property
  price: number;
  code: string;

};
